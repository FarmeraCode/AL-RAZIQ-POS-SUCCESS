{
  "name": "al-raziq-pos-server",
  "version": "1.0.0",
  "description": "Local LAN REST + WebSocket server for Al Raziq POS. Uses Node.js built-in SQLite (node:sqlite) — no native compilation, no Build Tools required.",
  "private": true,
  "type": "commonjs",
  "main": "index.cjs",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --experimental-sqlite index.cjs",
    "dev": "node --experimental-sqlite index.cjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
